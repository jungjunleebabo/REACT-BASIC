import React from "react";
import { useParams } from "react-router-dom";


function Detail() {
    // console.log("안녕핫[여")
    // const params = useParams();
    // return <span>{params.id}</span>;
    return <span>Detail this page</span>;
}

export default Detail;